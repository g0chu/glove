name: Labeler

on:
  pull_request_target:

permissions:
  labels: write
  pull-requests: write

jobs:
  labeler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v5
        with:
          dot-github: .github
